const params=new URLSearchParams(location.search);const cat=params.get('cat')||'Tecnología';const title=document.getElementById('categoryTitle');const intro=document.getElementById('categoryIntro');title.textContent=cat==='Ofertas'?'Ofertas y recomendaciones':cat;intro.textContent=cat==='Ofertas'?'Una selección de productos que merece la pena revisar.':`Nuestros productos destacados de ${cat.toLowerCase()}.`;let list=window.MEPILLO_PRODUCTS;if(cat!=='Ofertas')list=list.filter(p=>p.category===cat);document.getElementById('categoryGrid').innerHTML=list.map(card).join('');
