const slug=new URLSearchParams(location.search).get('p');const p=(window.MEPILLO_PRODUCTS||[]).find(x=>x.slug===slug)||(window.MEPILLO_PRODUCTS||[])[0];
function esc(v=''){return String(v).replace(/[&<>\"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[ch]))}
function featureList(items){return items?.length?`<ul class="feature-list-large">${items.map(x=>`<li><span class="feature-check">✓</span><span>${esc(x)}</span></li>`).join('')}</ul>`:''}
function specs(items){return items?.length?`<div class="spec-table">${items.map(x=>`<div><span>${esc(x.label||'Dato')}</span><strong>${esc(x.value||'')}</strong></div>`).join('')}</div>`:''}
async function render(){const catalog=await window.MepilloAmazon.getItem(p);const title=catalog.title||p.editorial.displayName;document.title=`${title} | MePillo`;const img=catalog.imageUrl||p.amazon.catalog.imageUrl;document.getElementById('productPage').innerHTML=`<div class="breadcrumbs"><a href="index.html">Inicio</a><span>›</span><a href="categoria.html?cat=${encodeURIComponent(p.category)}">${p.category}</a><span>›</span><strong>${esc(p.editorial.displayName)}</strong></div><div class="product-detail"><div class="detail-visual"><span class="product-badge">${p.badge}</span><img src="${esc(img)}" alt="${esc(title)}"></div><div class="detail-copy"><span class="product-page-kicker">RECOMENDACIÓN MEPILLO</span><h1>${esc(title)}</h1>${catalog.brand?`<p class="brand-line">Marca: <strong>${esc(catalog.brand)}</strong></p>`:''}<div class="score-row"><div class="detail-score">MePillo <strong>${p.editorial.score}/10</strong></div><span>Selección editorial</span></div><p class="detail-lead">${esc(p.editorial.summary)}</p><div class="best-for best-for-prominent"><strong>Ideal para</strong><span>${esc(p.editorial.bestFor)}</span></div><div class="quick-points">${p.editorial.why.slice(0,3).map(x=>`<span>✓ ${esc(x)}</span>`).join('')}</div><a class="primary-btn detail-buy" href="${p.amazon.directUrl}" target="_blank" rel="nofollow sponsored noopener">VER EN AMAZON.ES →</a><p class="affiliate-note">Enlace de afiliado. Precio y disponibilidad se comprueban directamente en Amazon.es.</p></div></div><section class="product-section"><div class="section-title-block"><span class="section-kicker">LO MÁS IMPORTANTE</span><h2>Características destacadas</h2><p>Lo que conviene saber antes de ir a Amazon.</p></div>${featureList(catalog.features)}</section><div class="product-info-grid"><section class="info-panel"><span class="section-kicker">FICHA RÁPIDA</span><h2>Datos y compatibilidad</h2>${specs(catalog.specifications)}</section><section class="info-panel"><span class="section-kicker">VEREDICTO MEPILLO</span><h2>¿Por qué lo recomendamos?</h2><ul class="editorial-list">${p.editorial.why.map(x=>`<li>✓ ${esc(x)}</li>`).join('')}</ul><div class="source-note">La imagen mostrada es una ilustración editorial de MePillo. Los datos oficiales podrán sincronizarse mediante Amazon Creators API cuando esté disponible.</div></section></div><section class="product-section"><div class="section-title-block"><span class="section-kicker">ANTES DE COMPRAR</span><h2>Pros y puntos a revisar</h2></div><div class="pros-cons"><div class="pros-box"><h3>Lo que nos gusta</h3><ul>${p.editorial.pros.map(x=>`<li><span>+</span>${esc(x)}</li>`).join('')}</ul></div><div class="cons-box"><h3>A tener en cuenta</h3><ul>${p.editorial.cons.map(x=>`<li><span>–</span>${esc(x)}</li>`).join('')}</ul></div></div></section>`;const related=(window.MEPILLO_PRODUCTS||[]).filter(x=>x.category===p.category&&x.slug!==p.slug).slice(0,5);document.getElementById('relatedGrid').innerHTML=related.map(card).join('')}render();